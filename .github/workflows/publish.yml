#
# Publishes OneXray across all supported platforms.
#
# Trigger:
#   - workflow_dispatch: manual trigger from the Actions tab
#   - push to a tag matching `v*`: cuts a release
#
# Required repository secrets (set under Settings → Secrets and variables → Actions):
#
# Convention: secrets whose value is a file or a multi-line string are stored
# base64-encoded (suffix `_BASE64`) so GitHub's secret editor never mangles
# newlines or whitespace. Simple single-line identifiers / passwords are stored
# as plain strings.
#
#   Apple (iOS / macOS / macos_se):
#     FASTLANE_ASC_KEY_ID               App Store Connect API key id
#     FASTLANE_ASC_ISSUER_ID            ASC issuer id (UUID)
#     FASTLANE_ASC_KEY_BASE64           base64-encoded .p8 contents
#     FASTLANE_ASC_KEY_PEM              raw .p8 contents (for apple-actions/download-provisioning-profiles,
#                                       which takes a string input — cannot be base64'd)
#     FASTLANE_MAS_INSTALLER_CERT       full subject of the 3rd Party Mac Developer Installer cert
#     APPLE_CERTS_P12_BASE64            base64 of a .p12 bundle containing ALL Apple code-signing certs
#                                       (Apple Distribution, 3rd Party Mac Developer Application,
#                                        Developer ID Application, 3rd Party Mac Developer Installer)
#     APPLE_CERTS_P12_PASSWORD          password used when exporting the .p12
#     KEYCHAIN_PASSWORD                 any non-empty string; used for the temporary build keychain
#
#   Android:
#     ANDROID_KEYSTORE_BASE64           base64 of the upload keystore (.jks)
#     ANDROID_KEYSTORE_PASSWORD         store password
#     ANDROID_KEY_PASSWORD              key password
#     ANDROID_KEY_ALIAS                 key alias
#     ANDROID_PLAY_SERVICE_JSON_BASE64  base64 of fastlane service account JSON
#
#   Firebase / Google Services (base64 of the file contents):
#     ANDROID_GOOGLE_SERVICES_JSON_BASE64
#     APPLE_GOOGLE_SERVICE_INFO_PLIST_BASE64      shared by iOS + MAS macOS (same bundle id net.yuandev.onexray)
#     MACOS_SE_GOOGLE_SERVICE_INFO_PLIST_BASE64   for the SE build  (swift/macOSSE/GoogleService-Info.plist)
#
#   AdMob (forwarded to Dart as --dart-define; App IDs consumed by Android
#   manifestPlaceholders / iOS Info.plist build setting):
#     ADMOB_APP_ID_ANDROID
#     ADMOB_APP_ID_IOS
#     ADMOB_AD_UNIT_ID_ANDROID
#     ADMOB_AD_UNIT_ID_IOS
#
# Layout:
#   build_scripts/app/builder.py#build_core expects the XTLS/libXray source checkout
#   to live as a sibling of this repo (same parent directory). Every job therefore
#   checks out this repo to `OneXray/` and libXray to `libXray/` at the workspace
#   root; fastforge/fastlane/flutter all produce their outputs under `<workspace>/output`.

name: Publish

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

env:
  GO_VERSION: "1.26.2"
  PYTHON_VERSION: "3.12"
  NDK_VERSION: r29
  LIBXRAY_REF: main
  BUILD_NUMBER: ${{ github.run_number }}

jobs:
  # =================================================================
  # macOS SE (Developer ID) — universal binary, Developer ID signed
  #   + notarized + zipped
  # =================================================================
  macos_se:
    runs-on: macos-26
    timeout-minutes: 60
    env:
      DEVELOPER_DIR: /Applications/Xcode_26.4.app/Contents/Developer
    defaults:
      run:
        working-directory: OneXray
    steps:
      - uses: actions/checkout@v6
        with:
          path: OneXray

      - uses: actions/checkout@v6
        with:
          repository: guodonn01/libXray
          ref: ${{ env.LIBXRAY_REF }}
          path: libXray

      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Setup Flutter
        run: bash build_scripts/setup_flutter.sh

      - name: Install Python build deps
        run: pip install pyyaml requests typer

      - name: Restore App Store Connect API key
        env:
          ASC_KEY_B64: ${{ secrets.FASTLANE_ASC_KEY_BASE64 }}
        run: echo "$ASC_KEY_B64" | base64 -d > macos_se/fastlane/AuthKey.p8

      - uses: apple-actions/import-codesign-certs@v6
        with:
          p12-file-base64: ${{ secrets.APPLE_CERTS_P12_BASE64 }}
          p12-password: ${{ secrets.APPLE_CERTS_P12_PASSWORD }}
          keychain-password: ${{ secrets.KEYCHAIN_PASSWORD }}

      - uses: apple-actions/download-provisioning-profiles@v5
        with:
          bundle-id: net.yuandev.onexray.se
          profile-type: MAC_APP_DIRECT
          issuer-id: ${{ secrets.FASTLANE_ASC_ISSUER_ID }}
          api-key-id: ${{ secrets.FASTLANE_ASC_KEY_ID }}
          api-private-key: ${{ secrets.FASTLANE_ASC_KEY_PEM }}
      - uses: apple-actions/download-provisioning-profiles@v5
        with:
          bundle-id: net.yuandev.onexray.se.tun
          profile-type: MAC_APP_DIRECT
          issuer-id: ${{ secrets.FASTLANE_ASC_ISSUER_ID }}
          api-key-id: ${{ secrets.FASTLANE_ASC_KEY_ID }}
          api-private-key: ${{ secrets.FASTLANE_ASC_KEY_PEM }}

      - name: Build, notarize & staple
        env:
          FASTLANE_ASC_KEY_ID: ${{ secrets.FASTLANE_ASC_KEY_ID }}
          FASTLANE_ASC_ISSUER_ID: ${{ secrets.FASTLANE_ASC_ISSUER_ID }}
          ADMOB_AD_UNIT_ID_ANDROID: ${{ secrets.ADMOB_AD_UNIT_ID_ANDROID }}
          ADMOB_AD_UNIT_ID_IOS: ${{ secrets.ADMOB_AD_UNIT_ID_IOS }}
        run: python3 build_scripts/main.py OneXray macos_se

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: macos_se
          path: output/OneXray-macos-universal*.zip
          if-no-files-found: error

  # =================================================================
  # Android — uploads AAB to Play Store via fastlane
  # =================================================================
  android:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    defaults:
      run:
        working-directory: OneXray
    steps:
      - uses: actions/checkout@v6
        with:
          path: OneXray

      - uses: actions/checkout@v6
        with:
          repository: guodonn01/libXray
          ref: ${{ env.LIBXRAY_REF }}
          path: libXray

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Setup Flutter
        run: bash build_scripts/setup_flutter.sh

      - name: Setup Android NDK
        # libXray's Android builder invokes `gomobile bind`, which needs an NDK.
        # nttld/setup-ndk pins the version and exports ANDROID_NDK_HOME/ANDROID_NDK_ROOT.
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: ${{ env.NDK_VERSION }}
          add-to-path: true

      - name: Install Python build deps
        run: pip install pyyaml requests typer

      - name: Restore Play Store service account
        env:
          PLAY_SA_B64: ${{ secrets.ANDROID_PLAY_SERVICE_JSON_BASE64 }}
        run: echo "$PLAY_SA_B64" | base64 -d > android/fastlane/playservice.json

      - name: Restore keystore
        env:
          KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
        run: |
          mkdir -p android/keystore
          echo "$KEYSTORE_B64" | base64 -d > android/keystore/upload.jks
          cat > android/keystore/keystore.properties <<EOF
          storeFile=../keystore/upload.jks
          storePassword=$KEYSTORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          EOF

      - name: Install fastlane
        run: sudo gem install fastlane --no-document

      - name: Build & upload
        env:
          ADMOB_APP_ID_ANDROID: ${{ secrets.ADMOB_APP_ID_ANDROID }}
          ADMOB_AD_UNIT_ID_ANDROID: ${{ secrets.ADMOB_AD_UNIT_ID_ANDROID }}
          ADMOB_AD_UNIT_ID_IOS: ${{ secrets.ADMOB_AD_UNIT_ID_IOS }}
        run: python3 build_scripts/main.py OneXray android

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: android-universal
          path: output/OneXray-android-universal*.apk
          if-no-files-found: error

  # =================================================================
  # Windows x64 — fastforge .zip + .exe
  #   Pinned to windows-2022 for Visual Studio 2022 (any edition —
  #   the runner ships Enterprise, VS 2022 Community is equivalent
  #   for C++ desktop workloads).
  # =================================================================
  windows:
    runs-on: windows-2022
    timeout-minutes: 60
    defaults:
      run:
        shell: bash
        working-directory: OneXray
    steps:
      - uses: actions/checkout@v6
        with:
          path: OneXray

      - uses: actions/checkout@v6
        with:
          repository: guodonn01/libXray
          ref: ${{ env.LIBXRAY_REF }}
          path: libXray

      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Setup Flutter
        run: bash build_scripts/setup_flutter.sh

      - name: Install Python build deps
        run: pip install pyyaml requests typer

      - name: Install fastforge
        run: |
          dart pub global activate fastforge
          echo "$LOCALAPPDATA/Pub/Cache/bin" >> "$GITHUB_PATH"

      - name: Build
        env:
          ADMOB_AD_UNIT_ID_ANDROID: ${{ secrets.ADMOB_AD_UNIT_ID_ANDROID }}
          ADMOB_AD_UNIT_ID_IOS: ${{ secrets.ADMOB_AD_UNIT_ID_IOS }}
        run: python build_scripts/main.py OneXray windows

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: windows-x64
          path: |
            output/OneXray-*.zip
            output/OneXray-*.exe
          if-no-files-found: error

  # =================================================================
  # Linux — fastforge .zip + .deb, x64 and arm64
  #   Pinned to ubuntu-24.04 (x64) / ubuntu-24.04-arm (arm64): the
  #   produced binary's glibc requirement is whatever the runner ships
  #   (glibc 2.39). Upgrading the runner image would silently bump the
  #   minimum glibc users need, so don't use `ubuntu-latest` here.
  #   ubuntu-24.04-arm is GitHub-hosted; free for public repos, paid
  #   for private ones.
  # =================================================================
  linux:
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    defaults:
      run:
        working-directory: OneXray
    steps:
      - uses: actions/checkout@v6
        with:
          path: OneXray

      - uses: actions/checkout@v6
        with:
          repository: guodonn01/libXray
          ref: ${{ env.LIBXRAY_REF }}
          path: libXray

      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Setup Flutter
        run: bash build_scripts/setup_flutter.sh

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ninja-build clang cmake pkg-config \
            libgtk-3-dev liblzma-dev libblkid-dev libsecret-1-dev \
            libayatana-appindicator3-dev \
            file

      - name: Install Python build deps
        run: pip install pyyaml requests typer

      - name: Install fastforge
        run: |
          dart pub global activate fastforge
          echo "$HOME/.pub-cache/bin" >> $GITHUB_PATH

      - name: Build
        env:
          ADMOB_AD_UNIT_ID_ANDROID: ${{ secrets.ADMOB_AD_UNIT_ID_ANDROID }}
          ADMOB_AD_UNIT_ID_IOS: ${{ secrets.ADMOB_AD_UNIT_ID_IOS }}
        run: python3 build_scripts/main.py OneXray linux

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: linux-${{ matrix.arch }}
          path: |
            output/OneXray-*.zip
            output/OneXray-*.deb
          if-no-files-found: error

  # =================================================================
  # Create a GitHub Release and attach non-store artifacts.
  # Only runs on tag pushes (v*), not on manual dispatch.
  # =================================================================
  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [ macos_se, android, windows, linux ]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/**/*
          fail_on_unmatched_files: false
          generate_release_notes: true
